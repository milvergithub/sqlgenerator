ngDescribe({
    name: 'Test ApplicationService',
    modules: 'app',
    inject: 'ApplicationService',
    tests: function (deps) {

        it('basic test', () => {
            //
        });
    }
});
