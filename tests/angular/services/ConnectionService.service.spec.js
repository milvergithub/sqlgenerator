ngDescribe({
    name: 'Test ConnectionServiceService',
    modules: 'app',
    inject: 'ConnectionServiceService',
    tests: function (deps) {

        it('basic test', () => {
            //
        });
    }
});
