ngDescribe({
    name: 'Test ConnectionService',
    modules: 'app',
    inject: 'ConnectionService',
    tests: function (deps) {

        it('basic test', () => {
            //
        });
    }
});
